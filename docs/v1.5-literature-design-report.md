# 记忆系统 v1.5 设计报告
## 基于文献调研的升级方案

**日期**: 2026-02-23  
**版本**: v1.4.0 → v1.5.0  
**参考文献**:
- ACT-R: Anderson (2007). *How Can the Human Mind Occur in the Physical Universe?*
- Stanford GA: Park et al. (2023). *Generative Agents: Interactive Simulacra of Human Behavior*
- RoboMemory: Lei et al. (2025). *RoboMemory: A Brain-inspired Multi-memory Agentic Framework*
- BMAM: Li et al. (2026). *BMAM: Brain-inspired Multi-Agent Memory Framework*

---

## 一、文献核心思想提炼

### 1. ACT-R（Anderson 2007）

认知架构鼻祖，核心贡献是**激活值理论**：

```
A(m) = B(m) + Σ W_i · S(i, m) + ε
```

- `B(m)`：基础激活值，随时间衰减，随访问频率增加
- `Σ W_i · S(i, m)`：**Spreading Activation**，当前工作记忆中的元素 i 通过关联强度 S 激活记忆 m
- `ε`：噪声项

关键洞见：**记忆不是孤立的，是通过关联网络互相激活的**。查到"Ktao"时，"河南中医药大学"、"灵兰项目"应该自动被激活。

### 2. Stanford Generative Agents（Park 2023）

25个AI角色模拟小镇，记忆架构三件套：

- **Observation**：原始事件流水账
- **Reflection**：定期对记忆做高层次总结（"我最近在做什么"、"我是什么样的人"）
- **Planning**：基于记忆规划行为

检索评分公式：
```
score = α·recency + β·importance + γ·relevance
```
三维加权，recency 用指数衰减，relevance 用语义相似度。

关键洞见：**Reflection 是记忆系统的"消化"过程**，没有 Reflection 的记忆系统只是在堆数据。

### 3. RoboMemory（Lei et al. 2025）

机器人具身智能，四类记忆**并行更新**：

| 记忆类型 | 对应脑区 | 存储内容 | 实现方式 |
|---------|---------|---------|---------|
| Spatial | 海马体 | 空间知识图谱 | 动态 KG，增量更新 |
| Temporal | 时间皮层 | 步骤摘要 FIFO | 线性缓冲区 |
| Episodic | 海马体 | 情节事件 | Vector DB |
| Semantic | 新皮层 | 语义事实 | Vector DB |

**Closed-Loop Planner + Critic**：规划→执行→Critic评估→重规划，形成闭环。

关键洞见：**并行更新减少延迟**；**Critic 模块实现自我纠错**。

### 4. BMAM（Li et al. 2026，最新）

提出 **Soul Erosion**（灵魂侵蚀）概念，直接命中我们的核心问题：

```
S(M_t) = α·T(M_t) + β·C(M_t) + γ·I(M_t)
```

- `T`：Temporal Coherence（时序一致性）——能否正确回答"什么时候发生的"
- `C`：Semantic Consistency（语义一致性）——同一实体的描述是否自相矛盾
- `I`：Identity Preservation（身份保持）——用户偏好、个性特征是否被保留

三种侵蚀类型及对应防御：

| 侵蚀类型 | 症状 | BMAM 防御机制 |
|---------|-----|-------------|
| Temporal Erosion | 搞混事件顺序 | StoryArc 时间线索引 |
| Semantic Erosion | 同一实体前后矛盾 | 海马→颞叶 consolidation |
| Identity Erosion | 忘记用户是谁 | 杏仁核显著性标签 |

混合检索（Hybrid Retrieval）：
```
RRF(d) = Σ 1/(k + rank_i(d))
```
四路融合：lexical + dense + knowledge-graph + temporal，用 Reciprocal Rank Fusion 合并。

---

## 二、我们系统现状 vs 文献差距分析

### 2.1 架构对比

```
我们的系统 (v1.4.0)          文献最佳实践
─────────────────────        ─────────────────────
Layer 1: 快照 (snapshot.md)  ≈ 工作记忆 (Working Memory)
Layer 2: facts/beliefs/      ≈ 语义记忆 (Semantic Memory)
         summaries (jsonl)
Layer 3: 原始日志            ≈ 情节记忆 (Episodic Memory)

缺失:
  - 时间线索引               ← BMAM StoryArc
  - 关联激活网络             ← ACT-R Spreading Activation
  - 自动 Reflection          ← Stanford GA
  - 混合检索                 ← BMAM Hybrid Retrieval
  - 健康度量指标             ← BMAM Soul Erosion Score
  - Critic 自我纠错          ← RoboMemory
```

### 2.2 检索机制对比

**现状**（`router_search()` 第1625行）：
```python
final_score = score * 0.4 + importance * 0.3 + memory_score * 0.3
```
问题：
- 没有 recency 维度——10天前的记忆和昨天的记忆权重一样
- 没有 spreading activation——查"灵兰"不会自动激活"Coze"、"Gemini"
- 没有 temporal 信号——时序引擎（v1.4.0）存在但没接入检索

**目标**（Stanford GA + BMAM）：
```python
final_score = α·recency(t) + β·importance + γ·relevance(q, m) + δ·spreading(m)
```

### 2.3 Consolidation 机制对比

**现状**：
- Phase 1-3：切片→筛选→提取（但 Phase 1 刚修好，之前断链）
- Phase 4：去重、冲突检测、实体更新
- Phase 4c：Summaries 生成——**条件是"无需生成摘要"，实际从未触发**

**目标**（Stanford GA Reflection）：
- 当某实体 facts 数量 > N 时，自动触发 Reflection，生成高层次 summary
- Reflection 内容："Ktao 最近在做什么"、"Ktao 对 XX 的态度是什么"

### 2.4 Soul Erosion 现状评估

对照 BMAM 的三维指标，评估我们系统当前状态：

| 维度 | 当前状态 | 风险等级 |
|-----|---------|---------|
| Temporal Coherence | timestamp 字段存在，但检索时未用 | 🟡 中 |
| Semantic Consistency | conflict_resolver 存在但弱 | 🟡 中 |
| Identity Preservation | MEMORY.md 手动维护，无自动保护 | 🔴 高 |

Identity Preservation 最危险——Ktao 的核心身份信息（医学生、灵兰、偏好）存在 MEMORY.md 里，但这个文件是静态的，不会随对话自动更新，也没有"保护机制"防止被低质量 facts 稀释。

---

## 三、升级方案设计

### 优先级矩阵

| 功能 | 来源 | 收益 | 改动量 | 优先级 |
|-----|-----|-----|-------|-------|
| 三维检索评分（recency+importance+relevance） | Stanford GA | 高 | 小 | P0 |
| 自动 Reflection（Phase 4c 触发） | Stanford GA | 高 | 小 | P0 |
| Identity 保护标签（is_identity 字段） | BMAM | 高 | 小 | P0 |
| Spreading Activation | ACT-R | 中 | 中 | P1 |
| Soul Erosion Score 监控 | BMAM | 中 | 中 | P1 |
| 混合检索 RRF | BMAM | 高 | 大 | P2 |
| Dense Vector 检索 | RoboMemory | 高 | 大 | P2 |
| Critic 自我纠错 | RoboMemory | 中 | 大 | P3 |

---

### P0：三维检索评分

**改动位置**：`scripts/memory.py` `router_search()` 约第1625行

**当前**：
```python
r["final_score"] = r.get("score", 0) * 0.4 + r.get("importance", 0.5) * 0.3 + r.get("memory_score", 0.5) * 0.3
```

**目标**：
```python
def compute_retrieval_score(record, query, now):
    # Recency: 指数衰减，半衰期 7 天
    days_ago = (now - parse_date(record["last_accessed"])).days
    recency = math.exp(-0.1 * days_ago)  # λ=0.1

    # Importance: 已有字段
    importance = record.get("importance", 0.5)

    # Relevance: 关键词重叠（现有逻辑）
    relevance = record.get("memory_score", 0.5)

    # 加权求和（参考 Stanford GA α=β=γ=1/3）
    return 0.35 * recency + 0.35 * importance + 0.30 * relevance
```

预期效果：最近访问的记忆排名更靠前，陈旧但不相关的记忆自然下沉。

---

### P0：自动 Reflection（Phase 4c 升级）

**改动位置**：`scripts/memory.py` Phase 4c，约第2630行

**触发条件**：某实体关联的 facts 数量 > 8 条，且距上次 summary > 7 天

**Reflection 内容模板**（无 LLM，纯规则）：
```python
def auto_reflect(entity, facts, memory_dir):
    """
    对某实体的 facts 做规则性摘要
    不调用 LLM，基于 importance 排序取 top-5 生成摘要
    """
    top_facts = sorted(facts, key=lambda f: f["importance"], reverse=True)[:5]
    summary_content = f"关于{entity}的综合认知（基于{len(facts)}条记录）：" + \
                      "；".join(f["content"][:50] for f in top_facts)
    return {
        "type": "summary",
        "content": summary_content,
        "entities": [entity],
        "source": "auto_reflection",
        "importance": max(f["importance"] for f in top_facts) * 0.9,
    }
```

---

### P0：Identity 保护标签

**改动**：在 facts.jsonl schema 中新增 `is_identity` 布尔字段

标记规则（Phase 2 筛选时）：
- 包含用户姓名、职业、核心项目、长期偏好的 facts → `is_identity: true`
- 检索时 identity facts 的 recency 衰减系数减半（更持久）
- consolidate Phase 5c 衰减时，`is_identity: true` 的记忆豁免衰减

---

### P1：Spreading Activation

**改动位置**：`router_search()` 检索后处理

**逻辑**：
```python
def spread_activation(results, all_facts, relations, spread_factor=0.3):
    """
    对检索结果中的实体，激活其关联实体的记忆
    """
    activated_entities = set()
    for r in results:
        activated_entities.update(r.get("entities", []))

    # 从 relations.json 找关联实体
    bonus_records = []
    for entity in activated_entities:
        related = relations.get(entity, [])
        for rel_entity in related[:3]:  # 最多扩散3跳
            for fact in all_facts:
                if rel_entity in fact.get("entities", []):
                    fact_copy = fact.copy()
                    fact_copy["final_score"] *= spread_factor
                    fact_copy["spread_from"] = entity
                    bonus_records.append(fact_copy)

    return results + bonus_records
```

---

### P1：Soul Erosion Score 监控

**新增脚本**：`scripts/soul_health.py`

```python
def compute_soul_score(memory_dir):
    """
    计算当前记忆系统的 Soul Score
    S = α·T + β·C + γ·I
    """
    # T: Temporal Coherence
    # 检查 facts 中 timestamp 字段完整率
    facts = load_facts(memory_dir)
    t_score = sum(1 for f in facts if f.get("timestamp")) / max(len(facts), 1)

    # C: Semantic Consistency
    # 检查 conflict_downgraded 比例（越低越好）
    conflict_count = sum(1 for f in facts if f.get("conflict_downgraded"))
    c_score = 1.0 - (conflict_count / max(len(facts), 1))

    # I: Identity Preservation
    # 检查 is_identity facts 的存活率和 importance 均值
    identity_facts = [f for f in facts if f.get("is_identity")]
    i_score = sum(f["importance"] for f in identity_facts) / max(len(identity_facts), 1) \
              if identity_facts else 0.5

    # 权重：identity 最重要
    soul_score = 0.25 * t_score + 0.35 * c_score + 0.40 * i_score
    return {
        "soul_score": round(soul_score, 3),
        "temporal_coherence": round(t_score, 3),
        "semantic_consistency": round(c_score, 3),
        "identity_preservation": round(i_score, 3),
        "risk": "HIGH" if soul_score < 0.6 else "MEDIUM" if soul_score < 0.8 else "LOW"
    }
```

接入 consolidate 报告，每次跑完显示健康分。

---

### P2：混合检索 RRF（未来版本）

参考 BMAM 的四路融合：

```
RRF_score(d) = Σ_i  1 / (k + rank_i(d))
```

四路：
1. **Lexical**：现有关键词匹配（已实现）
2. **Temporal**：temporal_engine 时序检索（已实现，未接入）
3. **Dense**：向量检索（v1.6.0 代码在，依赖未装）
4. **Knowledge Graph**：relations.json 图检索（部分实现）

需要先把 Dense 检索跑起来，再做 RRF 融合。

---

## 四、实施路线图

```
v1.5.0（近期，2-3天）
├── P0-1: 三维检索评分（recency + importance + relevance）
├── P0-2: 自动 Reflection（Phase 4c 触发条件）
└── P0-3: Identity 保护标签（is_identity 字段 + 衰减豁免）

v1.5.1（1周内）
├── P1-1: Spreading Activation
└── P1-2: Soul Erosion Score 监控 + consolidate 报告集成

v1.6.0（未来）
├── P2-1: Dense Vector 检索（修复现有 vector_embedding.py 依赖）
└── P2-2: 混合检索 RRF 四路融合

v1.7.0（远期）
└── P3: Critic 自我纠错模块（需要 LLM，成本高）
```

---

## 五、关键结论

1. **最紧迫的问题是 Identity Erosion**。Ktao 的核心身份信息没有保护机制，随着 facts 增多会被稀释。`is_identity` 标签 + 衰减豁免是最小代价的防御。

2. **检索是最容易提升的点**。加入 recency 维度几乎不需要改架构，但能显著改善"最近发生的事情"的召回质量。

3. **Reflection 是记忆系统的"消化"**。没有 Reflection，facts 只是在堆积，不会形成高层次认知。Phase 4c 的自动触发是补上这个缺口的关键。

4. **Soul Erosion Score 是健康监控的基础**。有了量化指标，才能知道系统在变好还是变坏。

5. **Dense 检索是长期竞争力**。纯关键词检索在语义相似但词汇不同的情况下会漏掉重要记忆，向量检索是必须补上的能力，但依赖问题需要单独解决。

---

*报告生成时间：2026-02-23*  
*下一步：确认 v1.5.0 实施范围后开始编码*
